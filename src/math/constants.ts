import { vec3, vec2 } from 'gl-matrix'

export const UP3 = vec3.fromValues(0, 1, 0)
export const ZERO3 = vec3.fromValues(0, 0, 0)
export const FORWARD3 = vec3.fromValues(0, 0, 1)
export const ZERO2 = vec2.fromValues(0, 0)